overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  src/graphql/types.ts:
    config:
      contextType: ./#Context
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"
